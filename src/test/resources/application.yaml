spring:
  sql:
    init:
      mode: always
  datasource:
    url: jdbc:h2:file:mem:filmorate
    username: sa
    password: password
    driverClassName: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect